/**
 * Represents the different endpoints exposed by the API.
 */
const endpoints = {
	ACCOUNTS: "/api/v1/accounts",
	AUTHENTICATE: "/api/v1/authenticate",
	ACCOUNT: "/api/v1/account",
};

export default endpoints;
